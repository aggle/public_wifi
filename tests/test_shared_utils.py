"""
This module contains tests for tr14.utils.shared_utils.py
"""
import pytest
from pathlib import Path
import random

import tr14.utils.shared_utils as shared_utils

def test_load_config_path():
    """make sure load_config_path returns paths that exist or throws exceptions"""
    # GIVEN:
    # WHEN:
    # THEN:
    # test that the path is an absolute path
    # test that the path is the correct absolute path? don't know how to do this
    pass
